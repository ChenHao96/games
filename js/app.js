function gameStart(canvasElement, canvasContext, loadResources, loadAudio) {

    const img = new Image()
    img.alt = "XXX游戏"
    img.src = "favicon.ico"

    return {
        "favicon.ico": img
    }
}